<%= @appliance.name %>



<%= button_to 'Back to Index', appliances_path, method: :get, class: 'btn btn-primary' %>



<%= link_to appliance_path(@appliance),
                      data: { turbo_method: :delete, turbo_confirm: "Are you sure?" },
                      class: "btn btn-danger" do %>
                      Delete
                  <% end %>
